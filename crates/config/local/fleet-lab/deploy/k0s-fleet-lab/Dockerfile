# Local build of product scripture image (requires BuildKit SSH for Holylog).
# Prefer building from the Scripture repo root:
#   DOCKER_BUILDKIT=1 docker build --ssh default -f deploy/kubernetes/Dockerfile -t scripture:0.1.0 .
FROM scripture:0.1.0
