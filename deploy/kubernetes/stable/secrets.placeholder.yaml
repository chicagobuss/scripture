# Secret *references* only — values belong in ignored overlay material.
# Create out-of-band, for example:
#   kubectl -n scripture-stable-REPLACE_RUN_ID create secret generic rustfs-credentials \
#     --from-literal=RUSTFS_ACCESS_KEY=... --from-literal=RUSTFS_SECRET_KEY=...
#   kubectl -n scripture-stable-REPLACE_RUN_ID create secret generic scripture-admin-token \
#     --from-literal=token=...
#
# Do not commit Secret YAML with stringData or data fields.
apiVersion: v1
kind: List
items: []
