# ServiceAccount for correctness-campaign Jobs. No cluster-admin, no secret
# create/list beyond what the Pod already mounts via secretRef.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: scripture-correctness
  namespace: scripture-correctness
  labels:
    app.kubernetes.io/name: scripture
    app.kubernetes.io/component: correctness-campaign
