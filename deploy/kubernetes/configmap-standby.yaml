apiVersion: v1
kind: ConfigMap
metadata:
  name: scripture-standby-config
  labels:
    app.kubernetes.io/name: scripture
    app.kubernetes.io/component: standby
data:
  scripture.yaml: |
    # Independent standby candidate. Requires existing Canon evidence.
    # Never self-promotes.
    version: 1
    node:
      owner_id: "scripture-own-b!"
      advertise: "tcp://scripture-standby:9000"
    listener:
      bind: "0.0.0.0:9000"
    verse:
      journal_id: "scripture-jrnl!!"
      verse_id: "scripture-verse!"
      cohort_id: "scripture-cohrt!"
      writer_id: "scripture-wrtr!!"
    store:
      backend: r2
      endpoint: "https://REPLACE_WITH_ENDPOINT"
      bucket: "REPLACE_WITH_BUCKET"
      region: auto
      prefix: "scripture/deployments/REPLACE_WITH_PREFIX"
    metrics:
      status_bind: "0.0.0.0:9100"
